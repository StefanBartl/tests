// test.ts

// Adds two numbers
function addNumbers(a: number, b: number): number {
  return a + b;
}

console.log("Hello, LSP Test in TypeScript!");
const sum = addNumbers(8, 12);
console.log(`8 + 12 = ${sum}`);
const a = 42;


